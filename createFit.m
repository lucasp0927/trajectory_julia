function [fitresult, gof] = createFit(xout, yout, zout)
%CREATEFIT(XOUT,YOUT,ZOUT)
%  Create a fit.
%
%  Data for 'untitled fit 1' fit:
%      X Input : xout
%      Y Input : yout
%      Z Output: zout
%  Output:
%      fitresult : a fit object representing the fit.
%      gof : structure with goodness-of fit info.
%
%  See also FIT, CFIT, SFIT.

%  Auto-generated by MATLAB on 24-Jun-2016 04:46:44


%% Fit: 'untitled fit 1'.
[xData, yData, zData] = prepareSurfaceData( xout, yout, zout );

% Set up fittype and options.
ft = fittype( 'poly44' );
opts = fitoptions( 'Method', 'LinearLeastSquares' );
opts.Lower = [-Inf -Inf -Inf -Inf 0 -Inf -Inf 0 0 -Inf -Inf 0 0 0 -Inf];
opts.Normalize = 'on';
opts.Upper = [Inf Inf Inf Inf 0 Inf Inf 0 0 Inf Inf 0 0 0 Inf];

% Fit model to data.
[fitresult, gof] = fit( [xData, yData], zData, ft, opts );

end

